version: '3.9'

services:
  postgres:
    image: postgres:17.4
    restart: always
    shm_size: 128mb

    environment:
      POSTGRES_PASSWORD: pass
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres

    ports:
      - "5434:5432"